<div class="row">
  <div class="col-md-6">
    <h2>{{title}}</h2>
      <div class="">
        <p for="body">{{body}}</p>
      </div>
      {{#if user_id}}
      <div class="form-group">
        <button class="btn btn-primary" type="create">edit post</button>
      </div>
  </div>
</div>

<script src="./js/login.js"></script>